# Project: my-first-project (GitHub Pages)

## 1. Core Directives (Strict Token Optimization)
- **CRITICAL:** DO NOT rewrite, refactor, or regenerate entire files unless explicitly asked.
- Only provide the specific code blocks that need to be changed (Diff format) or update directly via tools to save output tokens.
- Maintain existing logic and functionality. If a feature works perfectly, DO NOT touch it.
- Focus strictly on stability, fast loading times, and responsive UI.

## 2. Tech Stack & Environment
- **Platform:** Static Website hosted on GitHub Pages.
- **Languages:** HTML, CSS, and Vanilla JavaScript (or existing Frontend Framework in the repo).
- **Style:** Keep CSS minimal. Avoid adding heavy external libraries unless absolutely necessary.

## 3. GitHub & Deployment Workflow (Crucial for Stability)
- This project is deployed via GitHub Pages. Any changes pushed to the main/master branch will affect the live site.
- **Before Committing:** ALWAYS ensure syntax is correct and there are no missing closing tags or syntax errors that would break the UI.
- **Git Commands:** When asked to deploy, strictly follow these commands:
  1. `git add .`
  2. `git commit -m "Update: [Describe the specific change concisely]"`
  3. `git push`

## 4. Agent Behavior
- Do not explain basic coding concepts unless asked.
- Go straight to the solution or execute the update directly.